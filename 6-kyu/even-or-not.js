// Your code will determine if the number passed is even (or not). The function needs to return either a true or false. 
// Notes: Numbers may be positive or negative, integers or floats.

function testEven(n) {
    return (n % 2 == 0) ? true : false;
}